# Скрипт управления виртуальными машинами Proxmox

Универсальный bash-скрипт для автоматизации операций со снапшотами, бэкапами и управления виртуальными машинами в Proxmox VE.

## Возможности

- ✅ Создание снапшотов для диапазона ВМ
- ⏮️ Откат ВМ к снапшоту
- 🗑️ Удаление снапшотов
- 💾 Создание бэкапов
- 🔄 Восстановление из бэкапов
- 📋 Просмотр списка ВМ и снапшотов
- 📦 Просмотр доступных бэкапов
- 💥 Массовое удаление ВМ

## Синтаксис

```bash
./prox.sh ACTION START END [DESCRIPTION] [STORAGE] [BACKUP_ID]
```

### Параметры

| Параметр | Обязательный | Описание |
|----------|--------------|----------|
| `ACTION` | ✅ Да | Действие для выполнения |
| `START` | ✅ Да | Начальный ID виртуальной машины |
| `END` | ✅ Да | Конечный ID виртуальной машины |
| `DESCRIPTION` | ⚠️ Зависит | Описание снапшота/бэкапа |
| `STORAGE` | ⚠️ Зависит | Имя хранилища |
| `BACKUP_ID` | ❌ Нет | ID конкретного бэкапа |

## Доступные действия

### 1. create - Создание снапшотов

Создает снапшот для каждой ВМ в указанном диапазоне.

**Синтаксис:**
```bash
./prox.sh create START END "DESCRIPTION"
```

**Пример:**
```bash
./prox.sh create 200 232 "before_update"
```

**Особенности:**
- Автоматически генерирует уникальное имя снапшота с временной меткой
- Формат имени: `snap-YYYYMMDD-HHMMSS`
- Пропускает несуществующие ВМ

---

### 2. rollback - Откат к снапшоту

Откатывает ВМ к последнему снапшоту с указанным описанием.

**Синтаксис:**
```bash
./prox.sh rollback START END "DESCRIPTION"
```

**Пример:**
```bash
./prox.sh rollback 200 210 "before_update"
```

**Особенности:**
- Находит последний снапшот с указанным описанием
- Автоматически запускает ВМ после отката
- Если снапшот не найден, ВМ пропускается

---

### 3. delete - Удаление снапшотов

Удаляет все снапшоты с указанным описанием.

**Синтаксис:**
```bash
./prox.sh delete START END "DESCRIPTION"
```

**Пример:**
```bash
./prox.sh delete 200 232 "old_snapshot"
```

**Особенности:**
- Удаляет ВСЕ снапшоты, содержащие указанное описание
- Выводит количество найденных снапшотов перед удалением

---

### 4. backup - Создание бэкапа

Создает бэкап виртуальной машины в указанное хранилище.

**Синтаксис:**
```bash
./prox.sh backup START END "DESCRIPTION" STORAGE
```

**Пример:**
```bash
./prox.sh backup 200 205 "weekly_backup" local-zfs
```

**Параметры бэкапа:**
- Режим: `snapshot` (создание снапшота для консистентности)
- Сжатие: `zstd` (быстрое и эффективное сжатие)
- Удаление старых бэкапов: отключено (`--remove 0`)
- Описание сохраняется в метаданных бэкапа

---

### 5. restore - Восстановление из бэкапа

Восстанавливает ВМ из бэкапа. Поддерживает как автоматический выбор последнего бэкапа, так и восстановление из конкретного файла.

**Синтаксис:**
```bash
# Автоматическое восстановление из последнего бэкапа
./prox.sh restore START END "auto" STORAGE

# Восстановление из конкретного бэкапа
./prox.sh restore START END "" STORAGE "BACKUP_FILENAME"
```

**Примеры:**
```bash
# Восстановить последний бэкап ВМ 200
./prox.sh restore 200 200 "auto" local-zfs

# Восстановить из конкретного файла
./prox.sh restore 200 200 "" local-zfs "vzdump-qemu-200-2024_01_15-12_30_45.vma.zst"
```

**Особенности:**
- Если VMID не указан или равен `"auto"`, выбирается последний бэкап
- Целевое хранилище по умолчанию: `local` (можно изменить в функции `get_target_storage`)
- При наличии существующей ВМ запрашивает подтверждение на удаление
- Останавливает ВМ перед удалением
- После восстановления предлагает запустить ВМ
- Можно восстановить бэкап с другим VMID

**Процесс восстановления:**
1. Поиск бэкапа (автоматически или по имени файла)
2. Проверка существования целевого VMID
3. Подтверждение удаления (если ВМ существует)
4. Остановка и удаление существующей ВМ
5. Восстановление из бэкапа
6. Опционально: запуск восстановленной ВМ

---

### 6. listbackup - Просмотр бэкапов

Показывает все доступные бэкапы для ВМ в указанном хранилище.

**Синтаксис:**
```bash
./prox.sh listbackup START END STORAGE
```

**Пример:**
```bash
./prox.sh listbackup 200 210 local-zfs
```

**Вывод включает:**
- Имя файла бэкапа
- Размер бэкапа
- VMID виртуальной машины
- Список отсортирован от новых к старым

---

### 7. list - Просмотр ВМ и снапшотов

Выводит информацию о виртуальных машинах и их снапшотах.

**Синтаксис:**
```bash
./prox.sh list START END
```

**Пример:**
```bash
./prox.sh list 200 210
```

**Отображаемая информация:**
- ID виртуальной машины
- Текущий статус (running/stopped)
- Список всех снапшотов с описаниями

---

### 8. Destroy - Удаление ВМ

Удаляет все виртуальные машины в указанном диапазоне. **Требует подтверждения!**

**Синтаксис:**
```bash
./prox.sh Destroy START END
```

**Пример:**
```bash
./prox.sh Destroy 200 232
```

**⚠️ ВНИМАНИЕ:**
- Действие необратимо!
- Перед выполнением запрашивает подтверждение `yes`
- Удаляет ВМ вместе со всеми дисками и конфигурацией

---

## Работа с диапазонами

Скрипт поддерживает как прямые, так и обратные диапазоны:

```bash
# Прямой диапазон: 200, 201, 202, ..., 210
./prox.sh list 200 210

# Обратный диапазон: 210, 209, 208, ..., 200
./prox.sh list 210 200
```

## Обработка ошибок

Скрипт автоматически обрабатывает следующие ситуации:

- ✅ Пропуск несуществующих ВМ
- ✅ Проверка существования снапшотов перед откатом
- ✅ Проверка существования бэкапов перед восстановлением
- ✅ Валидация обязательных параметров
- ✅ Проверка формата ID виртуальных машин

## Статистика выполнения

После завершения операции скрипт выводит детальную статистику:

```
=========================================
📊 СТАТИСТИКА ВЫПОЛНЕНИЯ
=========================================
✅ Успешно: 10
⚠️  Пропущено: 2
❌ Ошибок: 0
📝 Всего обработано: 12 из 12
=========================================
```

**Счетчики:**
- **Успешно** - операции выполнены без ошибок
- **Пропущено** - ВМ не существует или операция не применима
- **Ошибок** - операции завершились с ошибкой

## Системные требования

- Proxmox VE 6.x или выше
- Bash 4.0+
- Права root или sudo для выполнения команд `qm`, `vzdump`, `pvesm`

## Используемые команды Proxmox

| Команда | Назначение |
|---------|------------|
| `qm status` | Проверка статуса ВМ |
| `qm snapshot` | Создание снапшота |
| `qm listsnapshot` | Список снапшотов ВМ |
| `qm rollback` | Откат к снапшоту |
| `qm delsnapshot` | Удаление снапшота |
| `qm start` | Запуск ВМ |
| `qm stop` | Остановка ВМ |
| `qm destroy` | Удаление ВМ |
| `qmrestore` | Восстановление из бэкапа |
| `vzdump` | Создание бэкапа |
| `pvesm list` | Список файлов в хранилище |

## Примеры использования

### Базовый workflow с обновлением

```bash
# 1. Создать снапшоты перед обновлением
./prox.sh create 200 210 "before_kernel_update"

# 2. Выполнить обновление на ВМ (вручную)
# ...

# 3. Если обновление прошло успешно - удалить снапшоты
./prox.sh delete 200 210 "before_kernel_update"

# 4. Если нужен откат - вернуться к снапшоту
./prox.sh rollback 200 210 "before_kernel_update"
```

### Регулярное резервное копирование

```bash
# Создать еженедельный бэкап всех production ВМ
./prox.sh backup 300 350 "weekly_$(date +%Y%m%d)" local-zfs

# Проверить созданные бэкапы
./prox.sh listbackup 300 350 local-zfs
```

### Миграция ВМ через бэкап

```bash
# 1. Создать бэкап ВМ 200
./prox.sh backup 200 200 "migration" local-zfs

# 2. Восстановить ВМ с новым ID 250
./prox.sh restore 250 250 "" local-zfs "vzdump-qemu-200-2024_01_15.vma.zst"
```

### Массовые операции

```bash
# Просмотр статуса всех тестовых ВМ
./prox.sh list 100 150

# Создание снапшотов для группы ВМ
./prox.sh create 100 150 "test_snapshot"

# Удаление группы тестовых ВМ
./prox.sh Destroy 100 150
```

## Безопасность

### Рекомендации

1. **Всегда создавайте бэкапы** перед критическими операциями
2. **Используйте описательные имена** для снапшотов и бэкапов
3. **Проверяйте диапазон** перед выполнением массовых операций
4. **Храните бэкапы** на отдельном хранилище
5. **Регулярно проверяйте** возможность восстановления из бэкапов

### Опасные операции

⚠️ **Destroy** - удаляет ВМ безвозвратно  
⚠️ **restore** - перезаписывает существующую ВМ  
⚠️ **rollback** - откатывает изменения после снапшота

## Настройка

### Изменение целевого хранилища

По умолчанию при восстановлении используется хранилище `local`. Чтобы изменить, отредактируйте функцию `get_target_storage`:

```bash
get_target_storage() {
    # Изменить на нужное хранилище
    local default_storage="local-ssd-seagate2tb"
    
    # Или добавить логику выбора по VMID
    # if [ $1 -ge 200 ] && [ $1 -le 299 ]; then
    #     echo "local-ssd"
    # else
    #     echo "local-zfs"
    # fi
    
    echo "$default_storage"
    return 0
}
```

## Устранение неполадок

### ВМ не находится

```bash
# Проверить список всех ВМ
qm list

# Проверить конкретную ВМ
qm status 200
```

### Бэкап не найден

```bash
# Проверить содержимое хранилища
pvesm list local-zfs

# Проверить бэкапы конкретной ВМ вручную
ls /mnt/pve/local-zfs/dump/ | grep "vzdump-qemu-200"
```

### Недостаточно прав

```bash
# Скрипт требует выполнения от root
sudo ./prox.sh list 200 210

# Или добавить пользователя в группу
usermod -aG sudo username
```

## Логирование

Скрипт выводит подробную информацию о каждой операции:

- 📸 Создание снапшота
- ⏮️ Откат к снапшоту
- 🗑️ Удаление
- 💾 Создание бэкапа
- 🔄 Восстановление
- ✅ Успех
- ⚠️ Предупреждение
- ❌ Ошибка

Для сохранения лога в файл:

```bash
./prox.sh create 200 210 "snapshot" 2>&1 | tee operation.log
```

## Лицензия

Скрипт предоставляется "как есть" без каких-либо гарантий.
